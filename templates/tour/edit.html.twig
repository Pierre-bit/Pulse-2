{% extends 'base.html.twig' %}

{% block title %}Modifier Tour{% endblock %}

{% block body %}
        	<!--  section -->
	<section class="page-info-section set-bg" data-setbg="img/page-info-bg.jpg">
		<div class="container">
			<div class="section-title text-center">
				<h2>Modifier Tour</h2>	
			</div>
		</div>
	</section>
	<!--  section end -->
    {{ include('tour/_form.html.twig', {'button_label': 'Mise à jour'}) }}
    </br></br>
    <a href="{{ path('tour_index') }}">Revenir à Tour</a>
{% endblock %}
