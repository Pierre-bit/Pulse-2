{{ form_start(form) }}
    <div class="contact-form">
        <div class="row">
            <div class="col-lg-4 offset-2">
                {{ form_row(form.titre) }}
            </div>
            <div class="col-lg-4">
                {{ form_row(form.artiste) }}
                
            </div>
        </div>
        <div class="row">
            <div class="col-lg-4 offset-4">
                {{ form_row(form.description) }}
            </div>
        </div>
        <button class="site-btn">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
